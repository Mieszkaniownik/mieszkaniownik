# Mieszkaniownik Docker Compose Configuration
# This file contains only Docker Compose infrastructure variables
# 
# Copy this file to .env and fill in your values
# 
# Application-specific variables should be configured in:
# - mieszkaniownik-backend/.env (backend configuration)
# - mieszkaniownik-frontend/.env (frontend configuration)

# =============================================================================
# DOCKER COMPOSE INFRASTRUCTURE
# =============================================================================

# PostgreSQL Configuration
POSTGRES_DB=mieszkaniownik
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Redis Configuration (optional password)
REDIS_PASSWORD=

# General Environment
NODE_ENV=development

# Database Seeding (set to false to skip seeding)
RUN_SEED=true

# Frontend Build Args (used during Docker build)
# These are only needed for the frontend Docker image build process
VITE_API_BASE_URL=http://localhost:5001
VITE_GOOGLE_MAPS_API_KEY=

# =============================================================================
# IMPORTANT: Application Configuration
# =============================================================================
# 
# For application-specific environment variables, edit:
# 
# Backend: mieszkaniownik-backend/.env
#   - JWT secrets
#   - Google OAuth credentials
#   - Email/Discord configuration
#   - Scraper settings
#   - API keys
#   - Feature flags
#   See: mieszkaniownik-backend/.env.example
#
# Frontend: mieszkaniownik-frontend/.env
#   - API endpoint (for local development)
#   - Google Maps API key (for local development)
#   See: mieszkaniownik-frontend/.env.example
#
# =============================================================================
