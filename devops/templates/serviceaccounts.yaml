apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-frontend-sa
  namespace: mieszkaniownik-dev
  labels:
    app: mieszkaniownik
    tier: frontend
    environment: dev
  annotations:
    description: 'Service account for mieszkaniownik frontend application in development'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-backend-sa
  namespace: mieszkaniownik-dev
  labels:
    app: mieszkaniownik
    tier: backend
    environment: dev
  annotations:
    description: 'Service account for mieszkaniownik backend API in development'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-database-sa
  namespace: mieszkaniownik-dev
  labels:
    app: mieszkaniownik
    tier: database
    environment: dev
  annotations:
    description: 'Service account for mieszkaniownik database in development'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-frontend-sa
  namespace: mieszkaniownik-prod
  labels:
    app: mieszkaniownik
    tier: frontend
    environment: prod
  annotations:
    description: 'Service account for mieszkaniownik frontend application in production'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-backend-sa
  namespace: mieszkaniownik-prod
  labels:
    app: mieszkaniownik
    tier: backend
    environment: prod
  annotations:
    description: 'Service account for mieszkaniownik backend API in production'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mieszkaniownik-database-sa
  namespace: mieszkaniownik-prod
  labels:
    app: mieszkaniownik
    tier: database
    environment: prod
  annotations:
    description: 'Service account for mieszkaniownik database in production'
    contact: 'wsparcie@mieszkaniownik.com'
automountServiceAccountToken: true
